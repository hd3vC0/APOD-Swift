# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'APOD-Swift' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for APOD-Swift
  pod 'Alamofire', '~> 5.5'

  target 'APOD-SwiftTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'APOD-SwiftUITests' do
    # Pods for testing
  end

end
